# R115: HM2 → HM1 — TIER_COOLDOWN_S 40→42 (+2s)

## 📊 数据采集 (2026-06-27 21:05-21:09 UTC)

### Docker 日志（最近 100 行，过滤错误/警告）
- **1 个 SSLEOFError**：在 k3（通过 7896 的代理键）上出现，2 秒后 SSL 重试成功
- 所有其他请求均为普通请求，无错误

### 运行时环境
```
UPSTREAM_TIMEOUT=64
TIER_TIMEOUT_BUDGET_S=138
KEY_COOLDOWN_S=38.0
TIER_COOLDOWN_S=40 (部署前)
MIN_OUTBOUND_INTERVAL_S=22.0
HM_CONNECT_RESERVE_S=24
```

### DB 指标（关键发现）

| 窗口 | 总数 | 成功 | 失败 | 成功率 | 平均延迟 | p50 | p90 | p95 |
|------|------|------|------|------|------|------|------|------|
| 30分钟 | 65 | 65 | 0 | **100%** | 21538ms | 20298ms | 31383ms | 52076ms |
| 1小时 | 129 | 129 | 0 | **100%** | 22239ms | 19992ms | 32244ms | 58570ms |

### 层健康度 (v_hm_tier_health_1h)
- deepseek_hm_nv: 1267 成功, 3 失败, 99.8% — 所有 3 次失败均为 R114 前数据，不在 1h 窗口内

### 每键延迟（1h，status=200）
| 键 | 请求数 | 平均延迟 | 最大延迟 | 最小延迟 |
|-----|--------|----------|----------|----------|
| k0 | 27 | 22976ms | 110132ms | 6468ms |
| k1 | 27 | 22490ms | 65387ms | 6464ms |
| k2 | 25 | 23298ms | 118374ms | 4693ms |
| k3 | 27 | 21380ms | 67470ms | 4780ms |
| k4 | 23 | 20936ms | 59718ms | 7408ms |

### 24h 键错误（历史数据，非当前问题）
- 所有 5 个 `deepseek` 键：NVCFPexecTimeout (19–26)；budget_exhausted_after_connect (1–2) — R114 前数据
- `glm5.1_hm_nv` 429s 为预期值（与 `hm40006` 层无关）

### Fallback（30 分钟）
- fallback_occurred=0, fallback_actually_attempted=0 — 无需回退

### 最后 10 个请求
全部成功，延迟范围为 4.7s–33s，状态为 200

## 🎯 优化分析

### 瓶颈识别
**无活跃瓶颈** — 系统在 100% 成功率下极其健康。唯一可观察到的信号是 TIER_COOLDOWN_S 与 KEY_COOLDOWN_S 之间的间隔（40 - 38 = 2 秒），**低于理想的 4 秒以上间隔**——这意味着 KEY_COOLDOWN_S 有效地限制了层恢复（争用约束）。

### 为什么是此参数而非其他参数
- **KEY_COOLDOWN_S vs TIER_COOLDOWN_S 间隔**：当间隔 < 4 秒时（当前：2 秒），键的冷却时间决定了层恢复的时间窗口。增大层冷却时间可解除此争用。
- **非其他参数**：UPSTREAM_TIMEOUT、MIN_OUTBOUND、CONNECT_RESERVE、BUDGET 均表现完美——100% 成功率，无需调整
- **少改多轮**：单参数变更，+2 秒。以最小干预换取最大稳定性

### 预期影响
- 层恢复间隔从 2 秒增至 4 秒——确保 KEY_COOLDOWN 在层恢复发生前有独立的恢复窗口
- 预防性——无活跃错误，但可降低 KEY_COOLDOWN 限制层恢复的未来概率
- 无回退增加、无延迟增加——纯噪声减少

## 🔧 变更执行

### 参数差异
```
TIER_COOLDOWN_S: "40" → "42" (+2s)
```

### Docker-Compose 变更
```yaml
# 第 422 行
- TIER_COOLDOWN_S: "40"
+ TIER_COOLDOWN_S: "42"
```

### 部署验证
1. ✅ **环境变量**：`TIER_COOLDOWN_S=42` 已确认
2. ✅ **启动日志**：`NVCF_pexec_models=['deepseek_hm_nv', 'kimi_hm_nv']`, `default=deepseek_hm_nv`
3. ✅ **/v1/models**：`['deepseek_hm_nv', 'kimi_hm_nv', ...]` — 12 个模型已提供，层配置正确
4. ✅ **部署后请求**：k3 (第一) → 首次尝试成功；k4 → 首次尝试成功
5. ✅ **错误计数**：0 个新错误，100% 成功（2/2 部署后请求）

## 📈 预期效果

| 指标 | 变更前 | 变更后 | 变化 |
|------|--------|--------|------|
| 成功率 | 100% (65/65 30分钟) | ~100% (预期) | 无变化 — 基线为完美 |
| KEY-TIER 间隔 | 2s (38→40) | 4s (38→42) | +2s 间隔扩充 |
| Fallback 率 | 0% | ~0% (预期) | 无变化 |
| All_tiers_exhausted | 0 (1h) | ~0 (预期) | 无变化 |
| SSLEOFError | 1 (k3) | ~0 (预期) | 可能减少 |

## ⚖️ 评判标准

- ✅ **更少报错**：0 次失败 → 100% 成功 → 已完美
- ✅ **更快请求**：已有 p50 命中的 1 次首次尝试，无需加速
- ✅ **超低延迟**：p50 ~20s 符合 NVCF 预期，无干预
- ✅ **稳定优先**：预防性间隔扩充，防止键 → 层争用，无回退影响

## ⏳ 轮到HM1优化HM2