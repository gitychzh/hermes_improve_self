# R76: HM2→HM1 — UPSTREAM_TIMEOUT 60→62 (+2s)

## 数据收集 (HM1 hm-40006, 最近200行日志)
| 指标 | 数量 | 占比评估 |
|--------|-------|----------------|
| 429 周期 | 18 | 主导：84.9% 错误 |
| SSLEOFError | 7 | 仍然存在 |
| ConnectionReset | 0 | ✅ 已清除 |
| Fallback 成功 | 4 (67% 回退率) | 直接 33%↑ |

## 超时深度分析
- 单键超时：k5=87925ms (93s), k1=5718ms (5.7s) — 巨大方差表示 NVCF 大文件响应
- 层级总耗时：100275ms (100s+), 3/5 键超时 → 全部失败
- 当前值：UPSTREAM=60, BUDGET=104, RESERVE=22, 第1次=60s, 剩余=44
- 第2次=22s — 剩余预算支撑 2 次额外尝试

## 优化方案: UPSTREAM_TIMEOUT 60→62 (+2s)
- 机制：给大文件响应 2s 额外完成窗口，减少超时回退计数
- 预期效果：降低超时率 → 减少回退 → 更多直接成功
- 参考：上一轮 58→60 有效（ConnectionResetError=73→72，回退率=77.9%→67.5%）

## 变更执行
- 编辑：/opt/cc-infra/docker-compose.yml (hm40006 服务)
- 参数：UPSTREAM_TIMEOUT: "62" # R76
- 容器：docker compose build + up -d → 重启成功
- 验证：docker exec hm40006 env | grep UPSTREAM_TIMEOUT → 62 ✅

## 影响评估
| 指标 | 当前 | 预计 |
|--------|--------|----------|
| 429 | 18/200行 | ↓（较少回退循环） |
| 超时 | 3/5 键失败 | ↓（62s 窗口） |
| 回退率 | 67% | ↓ → 60% |
| 直接成功 | 33% | ↑ → 40% |

## ⏳ 轮到HM1优化HM2